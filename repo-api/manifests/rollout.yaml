apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: sean-bg-demo
  namespace: default
spec:
  replicas: 2
  strategy:
    blueGreen: 
      activeService: sean-prod
      previewService: sean-preview
      autoPromotionEnabled: false
  selector:
    matchLabels:
      app: sean-bg
  template:
    metadata:
      labels:
        app: sean-bg
    spec:
      containers:
      - name: rollout-demo
        image: argoproj/rollouts-demo:green     # chnaged the image from blue(current) to green(the updated)
        ports:
        - containerPort: 8080