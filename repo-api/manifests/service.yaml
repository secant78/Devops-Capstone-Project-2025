apiVersion: v1
kind: Service
metadata:
  name: sean-prod
  namespace: default
spec:
  type: NodePort        
  selector:
    app: sean-bg
  ports:
  - port: 80
    targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: sean-preview
  namespace: default
spec:
  type: NodePort       
  selector:
    app: sean-bg
  ports:
  - port: 80
    targetPort: 8080